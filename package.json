{
  "name": "root",
  "private": true,
  "workspaces": [
    "applications/*"
  ],
  "devDependencies": {
    "lerna": "^4.0.0"
  },
  "scripts": {
    "cms": "lerna run develop --scope=cms --stream",
    "gomo": "lerna run start --scope=gomo --stream",
    "start": "lerna run develop --stream"
  }
}
